import React from "react";
import ReactDOM from "react-dom/client";
// reactdom

import App from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// main : nhan request tu index.html => goi app => reder noi dung app

// bai tap tao the ul li theo cu phap

// Component: ham , class co chu cái đầu tiên viết hoa -> <Tenham /> hoặc <Tenham></tenham>
